diff --git a/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraActivity.java b/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraActivity.java
index b0554fe..83e2c7d 100644
--- a/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraActivity.java
+++ b/node_modules/@capacitor-community/camera-preview/android/src/main/java/com/ahm/capacitor/camera/preview/CameraActivity.java
@@ -286,9 +286,9 @@ public class CameraActivity extends Fragment {
 
                     private void handleZoom(MotionEvent event, Camera.Parameters params) {
                         if (mCamera != null) {
-                            mCamera.cancelAutoFocus();
                             int maxZoom = params.getMaxZoom();
-                            int zoom = params.getZoom();
+                            int oldZoom = params.getZoom();
+                            int zoom = oldZoom;
                             float newDist = getFingerSpacing(event);
                             if (newDist > mDist) {
                                 //zoom in
@@ -298,8 +298,15 @@ public class CameraActivity extends Fragment {
                                 if (zoom > 0) zoom--;
                             }
                             mDist = newDist;
-                            params.setZoom(zoom);
-                            mCamera.setParameters(params);
+                            // PATCH: only touch the camera when the zoom level ACTUALLY changes.
+                            // At the floor (0) or ceiling (maxZoom) the value is unchanged, and
+                            // calling cancelAutoFocus()/setParameters() on every touch-move there
+                            // hiccups the Camera1 preview surface → momentary gray flash on de-zoom.
+                            if (zoom != oldZoom) {
+                                mCamera.cancelAutoFocus();
+                                params.setZoom(zoom);
+                                mCamera.setParameters(params);
+                            }
                         }
                     }
                 }
